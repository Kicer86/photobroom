<configuration>

	-- introduce known configuration keys
	<keys>
		<key name="Application::ConfigDir" />    -- base path for configurations, databases etc
	</keys>

	-- default values
	<defaults>

	</defaults>

</configuration>
