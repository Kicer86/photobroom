<configuration>

	-- introduce known configuration keys
	<keys>
		<base />          -- base path for configurations, databases etc
	</keys>

	-- default values
	<defaults>
		<base path="">    -- default path for configuration is unknown at compilation time
	</defaults>
	
</configuration>
