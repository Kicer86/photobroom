
scalar DateTime
scalar Date
scalar Time

type Photo {
  id: ID!
  createdAt: DateTime!
  tags: Tags!
}

type Tags {
    date: Date
    time: Time
    event: String
}

type Query {
  photos: [Photo!]!
  photo(id: ID!): Photo
}
