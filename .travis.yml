language: cpp

branches:
  only:
    - travis_test

notifications:
    on_failure: never
    on_success: never
    
addons:
  apt:
    packages:
      - qtbase5-dev
    sources: &sources
      - ubuntu-sdk-team
    
before_script:
  - mkdir build
  - cd build
  - cmake ..

script: make
